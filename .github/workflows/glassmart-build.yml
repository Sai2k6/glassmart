name: Glassmart build

on:
  push:
    branches: ["main", "feature/complete-glassmart"]
  pull_request:
    branches: ["main"]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: glassmart
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: glassmart/package-lock.json
      - name: Connect hardware catalogue to Supabase
        run: python scripts/connect_hardware_supabase.py
      - name: Commit generated hardware integration
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/App.tsx src/hardwareSupabase.ts scripts/connect_hardware_supabase.py
          git diff --cached --quiet || git commit -m "Connect hardware catalogue to Supabase [bot-fix]"
          git push
      - run: npm ci
      - run: npm run build
